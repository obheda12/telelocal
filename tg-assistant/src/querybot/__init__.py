"""
Querybot package — Telegram bot that answers natural-language questions
about synced messages using hybrid search and Claude.

The bot runs under a dedicated system user (tg-querybot) and accepts
commands only from the configured owner_id.
"""
